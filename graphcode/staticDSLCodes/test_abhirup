function compute_static_TC(Graph g){
    int totalMatching = 0;
    forall(v in g.nodes()){
        if(v.rmatch != -1){
            totalMatching = totalMatching + 1;
        }
    }
    return totalMatching;
}
